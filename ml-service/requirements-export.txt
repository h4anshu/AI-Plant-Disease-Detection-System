# Building the ONNX files (train/export_onnx.py), the parity gate (train/check_onnx_parity.py) and
# OOD calibration (train/calibrate_ood.py), the relabel export (train/export_relabel_queue.py).
# Not needed to serve. Install on top of requirements.txt.
tensorflow==2.21.0  # the version that saved models/**/*.keras
tf2onnx==1.17.0
onnx==1.23.0
matplotlib==3.11.1
scikit-learn==1.9.0
pymongo==4.18.2
